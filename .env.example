# Fill out these lines before starting
DISCORD_TOKEN=YOUR_DISCORD_TOKEN
ANNOUNCE_CHANNEL_ID=YOUR_ANNOUNCE_CHANNEL_ID

# Send interval in minutes (default: 1)
CHECK_INTERVAL_MINUTES=1

# Optional: if you hit rate limits, add a classic token.
# GitHub will treat you as authenticated and raise limits.
GITHUB_TOKEN=